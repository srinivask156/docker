version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello Circleci!'
            echo 'This is the delivery pipeline'
      - run:
         name: Code is here
         command: |
            ls -al
            echo 'Looks similar'
      - run:
         name: running in a unique container
         command: |
           node -v
